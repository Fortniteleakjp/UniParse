<UserControl x:Class="UniParse.Controls.Model3DViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FF202225" ClipToBounds="True" Focusable="True">
    <Grid>
        <Viewport3D x:Name="Viewport">
            <Viewport3D.Camera>
                <PerspectiveCamera x:Name="Camera" FieldOfView="45"/>
            </Viewport3D.Camera>
            <ModelVisual3D>
                <ModelVisual3D.Content>
                    <Model3DGroup>
                        <AmbientLight Color="#FF606060"/>
                        <DirectionalLight Color="#FFFFFFFF" Direction="-1,-1,-2"/>
                        <DirectionalLight Color="#FF7A7A7A" Direction="1,1,2"/>
                    </Model3DGroup>
                </ModelVisual3D.Content>
            </ModelVisual3D>
            <ModelVisual3D x:Name="MeshVisual"/>
        </Viewport3D>
        <TextBlock Text="ドラッグ: 回転   /   ホイール: ズーム" Foreground="#80FFFFFF" FontSize="11"
                   HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="8"/>
    </Grid>
</UserControl>
